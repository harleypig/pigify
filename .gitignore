.env
.env.local

# Secrets directory - ignore secret files but keep structure
secrets/*.txt
!secrets/.gitkeep
!secrets/README.md

# SSL certificates for local development
certs/*.pem
certs/*.key
!certs/.gitkeep
