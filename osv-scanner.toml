# osv-scanner allowlist — reviewed, accepted findings. Each needs a reason.
# See the global rules/osv-scanner.md.
#
# Currently empty: the previously-allowlisted findings were all resolved by
# dependency bumps —
#   - starlette runtime CVE: bumped to 1.2.x (FastAPI 0.136 compatible).
#   - esbuild / vite / vitest (dev only): cleared by the Vite 8 + Vitest 4
#     upgrade.
# Add new entries here (with a reason) only when a finding is genuinely
# accepted rather than fixed.
